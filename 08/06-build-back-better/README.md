# Build back better

Combining the builder pattern with type state!

## Overview

This is the code for the 'type state' example from chapter 8.

Search for `MarkerTraitForBuilder` in the source code.

Macro generated and manually cleansed code () in `builder-code/src/res.rs`.

